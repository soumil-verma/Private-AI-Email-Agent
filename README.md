# Private Email Agent

Flutter macOS application for the Phase 1 private email workflow:
triage, understand, and prepare a local reply.

## Verify

```sh
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
flutter build macos --debug
```
